---
name: TestTarget
description: A test skill for processing and executing tasks. USE WHEN test target OR do something OR run task OR execute task OR process task OR perform task OR run something OR do a task OR handle task OR run test target OR execute something OR carry out task.
---
# TestTarget

A simple test skill for evaluation purposes.

## Workflow Routing

When a workflow is matched, **read its file and follow the steps within it.**

| Workflow | Trigger | File |
|----------|---------|------|
| **DoSomething** | "do something", "run task" | `Workflows/DoSomething.md` |
