# CodeReview Orchestrator

> You are a thin orchestrator. You spawn agents and check artifacts.
> Agents read their own workflow files and write their own findings.
> Your job: pass file paths, check artifacts exist, move to next step.

## Step 0: Resolve Paths

```bash
REPO_ROOT=$(git rev-parse --show-toplevel)
SKILL_ROOT=$REPO_ROOT/skills/CodeReview
```

## Path Conventions

`$REVIEW_DIR` follows the PAI context system convention:
  `_output/contexts/[context-slug]/reviews/codereview/[YYYYMMDD-HHMMSS]`

The `[context-slug]` is auto-generated by the PAI context hook (UserPromptSubmit) from the user's prompt text — the orchestrator does not derive it. It appears in the hook's output as `Context folder: [path]`. Use this path directly.

If no context hook output is available, create one: `YYYYMMDD-HHMM-codereview-[branch-name]`.

## Step 1: Setup

Determine review mode from the user's request (diff vs audit). Then create the review directory:

```bash
mkdir -p $REVIEW_DIR
```

## Step 2: Gather Context

Spawn agent:
- **prompt:** `"Read $SKILL_ROOT/Workflows/GatherContext.md and execute it. Review target: [commit range or target path from user]. Requested lenses: [any /SkillName arguments]. Write your output to: $REVIEW_DIR/context.md. When done, return the absolute path to context.md."`
- **subagent_type:** `general-purpose`

**CHECK:** Read `$REVIEW_DIR/context.md` — must exist and be non-empty.
- If missing → retry once with same prompt.
- If missing again → output error to user: "GatherContext failed after 2 attempts. Review aborted."

## Step 3: Select Dimensions

Spawn agent:
- **prompt:** `"Read $SKILL_ROOT/Workflows/SelectDimensions.md and execute it. Context file: $REVIEW_DIR/context.md. Dimensions directory: $SKILL_ROOT/Dimensions/. Write your output to: $REVIEW_DIR/dimensions.json. When done, return the absolute path to dimensions.json."`
- **subagent_type:** `general-purpose`

**CHECK:** Read `$REVIEW_DIR/dimensions.json` — must exist and contain a JSON array of dimensions.
- If missing → retry once. If missing again → error to user.

## Step 4: Spawn Review Agents

Read `$REVIEW_DIR/dimensions.json` — this is the only structured artifact the orchestrator parses, besides collecting agent output file paths.

For each dimension in the list, spawn an agent in parallel (`run_in_background: true`):

- **subagent_type:** `general-purpose`
- **prompt:**

```
You are a code reviewer for one specific dimension.

## Your Review Rules
Read [dimension.path] for your detection heuristics, severity calibration, and output format.

## Review Context
Read $REVIEW_DIR/context.md for what changed, why, and the review scope.

## Files to Review
[dimension.files]

## Instructions
1. Read both files above before reviewing any code
2. Review each file against your dimension's heuristics
3. For each issue found, record: severity, file, line, heuristic used, description, recommendation
4. Write all findings to: $REVIEW_DIR/dimension-[dimension.id].md
5. When done, return the absolute path to the file you wrote
```

**Synchronization:** Store each agent's task_id and dimension id. After all spawned:
1. Call `TaskOutput(task_id, block=true)` for each agent to collect the file path it returned
2. Per-agent check: verify the returned path exists and is non-empty. If missing, retry that specific agent once
3. Verify: collected path count matches `dimensions.json` entry count
4. Build the list of agent output paths for downstream steps

The orchestrator collects file paths only — it does not read agent output content.

## Step 5: Verify Claims

Spawn agent:
- **prompt:** `"Read $SKILL_ROOT/Workflows/VerifyClaims.md and execute it. Agent output files: [list of $REVIEW_DIR/dimension-*.md paths from Step 4]. Context: $REVIEW_DIR/context.md. Write output to: $REVIEW_DIR/verified-findings.md. When done, return the absolute path to verified-findings.md."`
- **subagent_type:** `general-purpose`

**CHECK:** `$REVIEW_DIR/verified-findings.md` exists.

## Step 6: Generate Report

Spawn agent:
- **prompt:** `"Read $SKILL_ROOT/Workflows/GenerateReport.md and execute it. Verified findings: $REVIEW_DIR/verified-findings.md. Context: $REVIEW_DIR/context.md. Write output to: $REVIEW_DIR/report.md. Return the report content AND the path."`
- **subagent_type:** `general-purpose`

Output the report to the user.
